import { styled } from "stitches.config";

const HorizontalLine = styled("span", {
  height: "2px",
  backgroundColor: "#fff",
  display: "block",
  width: "100%",
});

export default HorizontalLine;
